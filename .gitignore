node_modules
dist
dist-ssr
*.local
.DS_Store
.vite

# Environment secrets — never commit these
.env
.env.*
!.env.example

# Legacy SQLite files (superseded by Postgres)
server/*.sqlite
server/*.sqlite-shm
server/*.sqlite-wal

# OneDrive / cloud conflict-copy artifacts (e.g. "Profile-aidan.tsx").
# These are device-name duplicate copies, never real source — keep them out.
*-aidan.*
